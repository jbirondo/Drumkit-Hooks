import React, { useState } from 'react'

function DrumButton(props){

    useEffect(() => {
        
    })

    return(
        <button onClick={this.drumClick} >Hello World</button>
    )
}

export default DrumButton